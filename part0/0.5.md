```mermaid
flowchart TD
    A[The browser fetches the html css javascript and json files all at once]
   -.->|The browser renders the actual notes list| B[The app/page is ready to start working/creating-adding notes]
```
